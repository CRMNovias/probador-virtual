/**
 * Formatters - TODO: Implement formatting utilities
 */

export const formatPhoneNumber = (phone: string): string => phone;
export const formatDate = (date: string | Date): string => String(date);
export const formatTime = (time: string | Date): string => String(time);
export const formatRelativeTime = (date: string | Date): string => String(date);
export const formatFileSize = (bytes: number): string => bytes + ' bytes';

